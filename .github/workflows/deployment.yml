name: deployment

on:
  workflow_run:
    workflows: [preflight]
    branches: [master, main]
    types:
      - completed

jobs:
  dependencies:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [12.x]

    steps:
      - uses: actions/checkout@v2

      - name: deployment
        run: |
          echo "${{ github.event.workflow_run }}"
